import * as commentsUtils from '../commentsUtils';

it('noop', () => {});

// const testCommentLine1 = `
//  @param {ScaffoldedComponentSubmitTypes1}
// `;
//
// const testCommentLine2 = `
// * Submit the entered value
// *
// * @param {ScaffoldedComponentSubmitTypes2}
// `;
//
// it('parse comments', () => {
//   let result = commentsUtils.getWcdAnnotations(testCommentLine1);
//   result = {...result, ...commentsUtils.getWcdAnnotations(testCommentLine2)};
//   console.info(JSON.stringify(result, null, 4));
// });
