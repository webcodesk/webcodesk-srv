import FlowDebuggerManager from '../FlowDebuggerManager';

it('noop', () => {});

// it('loading flow model', () => {
//   const flowDebuggerManager = new FlowDebuggerManager(asctionSequences);
//   // const flowModel = flowDebuggerFactory.createFlowModelByActionSequences(asctionSequences);
//   const orderedLog = flowDebuggerManager.setDataFromLog(actionsLog);
//
//   console.info('Log: ', JSON.stringify(orderedLog, null, 2));
//
//   console.info('FlowModel: ', JSON.stringify(flowDebuggerManager.getFlowModel(), null, 4));
//
// });